/** Render-side caps from the performance budget (§13). */
export const CAP_PARTICLES = 600;
export const CAP_DRAW_WARN = 90;

/** Scar layer resolution. Coarse on purpose - it is a smear, not a decal. */
export const SCAR_W = 512;
export const SCAR_H = 256;
